函数式接口
1. 我们可以将lambda表达式当作任意只包含一个抽象方法的接口类型，确保你的接口一定达到这个要求，你只需要给你的接口添加 @FunctionalInterface
2. 方法与构造函数的引用，使用符号：::
3. lambda表达式中访问外部变量:
   lambda表达式中访问的局部变量必须是final或隐式final的（即不允许修改）
   lambda表达式中访问的类的成员变量是允许修改的 